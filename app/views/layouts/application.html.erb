<!DOCTYPE html>
<html>
  <head>
    <title>AssetExplorer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if !session[:logged_in] %>
        <%= link_to image_tag("/images/eve-sso-login-black-large.png"), controller: 'oauth', action: :login %>
    <% else %>
        <p>Logged in as <%= current_user %>, <%= link_to "Log out", controller: "oauth", action: :logout %><p>
    <% end %>
    <%= yield %>
  </body>
</html>
